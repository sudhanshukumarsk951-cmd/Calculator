# Calculator
This is a  Calculator application implemented in Python using the tkinter library for the graphical user interface (GUI). It also utilizes the built-in math, json, os, and re modules for mathematical operations, settings management, file handling, and regular expression parsing, respectively.
